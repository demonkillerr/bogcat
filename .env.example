# Database
DATABASE_URL="file:./dev.db"

# Auth — bcrypt hash of the shared password (generate with: node -e "console.log(require('bcryptjs').hashSync('yourpassword', 12))")
HASHED_PASSWORD=""

# JWT — use a strong random secret in production
JWT_SECRET="bogcat-jwt-secret-change-in-prod"

# App URLs
WEB_URL="http://localhost:3000"
PORT=4000
